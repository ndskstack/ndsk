export default (props)=>{  
    return (
      <html lang="en">
        <head>
          <title>blog</title> 
        </head>
        <body> 
            {props.children}
        </body>
      </html>
    );
}
