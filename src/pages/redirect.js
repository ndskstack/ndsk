export default (props)=>{
    return (
        <html lang="en">
          <head>
            <title>redirect</title> 
          </head>
          <body> 
              {props.name}
          </body>
        </html>
      );
}