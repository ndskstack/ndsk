export default (props)=>{  
    return (
      <html lang="en">
        <head>
          <title>SSG</title> 
        </head>
        <body> 
            {props.title} 
        </body>
      </html>
    );
}
