export default (props)=>{  
    return (
      <html lang="en">
        <head>
          <title>index</title> 
        </head>
        <body> 
            {props.children}
        </body>
      </html>
    );
}
